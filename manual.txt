export FLASK_APP=app.py
flask run

export FLASK_DEBUG=1
flask run

import secrets
secrets.token_hex(16)


from webappflask import db, create_app
from webappflask.models import User, Post, Item, Recipe
db.create_all(app=create_app())

user_1 = User(username='Vit', email='vit@g.com', password='password')
db.session.add(user_1)
user_2 = User(username='Vit2', email='vit2@g.com', password='password')
db.session.add(user_2)
db.session.commit()

User.query.all()
User.query.filter_by(username='Vit').all()
User.query.filter_by(username='Vit').first()

user = User.query.filter_by(username='Vit').first()
user.id

user = User.query.first()
user.password


db.drop.all()
db.create.all()


pipenv install -r requirements.txt
pip freeze --local > requirements.txt


touch .env (in project directory)
open -e .env (add it to .gitignore)
